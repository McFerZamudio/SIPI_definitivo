@model IEnumerable<SIPI_web.Models.tbl_informeAcademicoEstatus>

<table>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.informeAcademicoEstatus_nombre)
                </td>
            </tr>
        }
    </tbody>
</table>
