@model IEnumerable<SIPI_web.Models.tbl_sede>

<table>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.sede_nombre)
                </td>
            </tr>
        }
    </tbody>
</table>
