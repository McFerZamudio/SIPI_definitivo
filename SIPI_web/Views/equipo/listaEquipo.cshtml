@model IEnumerable<SIPI_web.Models.tbl_equipo>

<table>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.equipo_nombre)
                </td>
            </tr>
        }
    </tbody>
</table>
