@model IEnumerable<SIPI_web.Models.tbl_pai>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutPublico.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.pais_nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.pais_nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_pais">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_pais">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_pais">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
