@model IEnumerable<SIPI_web.Models.tbl_ciudad>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutPublico.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ciudad_nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_estadoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ciudad_nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_estadoNavigation.estado_nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_ciudad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_ciudad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_ciudad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
